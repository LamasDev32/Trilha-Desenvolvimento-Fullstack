/* 

Variáveis espaços em memória rotuladas para guardar um determinado valor.

var - variável de escopo global (não utilizada mais atualmente)
let - variavel de escopo de bloco (variaveis que variam)
const - variaveis constantes ou seja que recebem valores que não serão alterados em tempo de execução.

declaração do tipo + nome da variável = informação armazenada

let mensagem = 'Olá, mundo!'
console.log(mensagem)

let cor = 'vermelho'
console.log(cor)

cor = 'azul' // reatribuição correto
console.log(cor)

const segundaMensagem = 'Bem-vindo ao curso impressionador!'
console.log(segundaMensagem)  

var nome = 

*/