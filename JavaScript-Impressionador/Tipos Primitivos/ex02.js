/* 

Modifique o programa anterior para armazenar a mensagem em uma variável antes de exibi-la no console.

*/

let nome = "Felipe Lamas";
let mensagem = `Seja bem-vindo, ${nome}`;

console.log(mensagem);
