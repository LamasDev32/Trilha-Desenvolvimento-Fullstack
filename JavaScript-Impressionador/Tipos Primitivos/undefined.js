let mensagem = "Olá impressionador";
let preco = 50;
let logado = true;

//Undefined - Utilizar para variaveis de controle ou interpretar como retorno de um erro em algum ponto do código. 
let produto;
let carrinho = undefined;
console.log(typeof produto);
console.log(typeof carrinho);

//Null - Utilizar para validações de retorno exemplo o preenchimento de um formulário que precisa de forma obrigatória receber um e-mail válido (campo não pode estar vazio.)
let lista = null;
console.log(lista);
console.log(typeof lista);